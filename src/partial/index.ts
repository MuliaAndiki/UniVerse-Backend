export interface ICampusContact {
  email?: string;
  phone?: string;
}

export interface IOrganizerProfile {
  bio?: string;
}

export interface IUserProfile {
  fullName?: string;
  phone?: string;
}
